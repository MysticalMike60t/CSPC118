[project]
name = "cs118_Hw2"
version = "1.0.0"
description = "Homework #2 for CS118."
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cs118_Hw2 = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src", "lib"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
