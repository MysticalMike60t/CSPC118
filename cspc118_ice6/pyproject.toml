[project]
name = "cspc118-ice6"
version = "0.1.0"
description = "In Class Exercise Lect 6"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cspc118_ice6 = "src:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src", "lib"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
