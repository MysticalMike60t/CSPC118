[project]
name = "cspc118_hw2"
version = "1.0.0"
description = "Homework #2"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cspc118_hw2 = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src", "lib"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
